on:
  push:
    branches:
      - main  # Trigger workflow on push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Write permission for repository contents
      pages: write     # Write permission for GitHub Pages
      id-token: read   # Allow to authenticate with GitHub's ID token
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install MkDocs and plugins
        run: |
          pip install mkdocs mkdocs-material

      - name: Build the MkDocs site
        run: |
          mkdocs build

      - name: Deploy to GitHub Pages
        run: |
          mkdocs gh-deploy --force
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}  # Use your custom token
